/*
 * entrypoint for the lazy_config api
 * only the functions you need and all the functions you need should be here.
 */

#include "lazy_config.h"
#include <stdio.h>

/*
 * read_config()
 * takes a pointer to a struct and rewrites it with config values.
 * Any values that are missing will assume default values.
 *
 * returns 0 on success.
 */
int read_config(){
  return 0;
}

/*
 * write_config()
 * takes a pointer to a struct and write the contents to a config file
 *
 * returns 0 on success
 */
int write_config(){
  return 0;
}
